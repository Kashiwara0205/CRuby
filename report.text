1 + 1や1 - 1などの単純な計算は純粋なrubyの処理速度が勝る
- 5/30

より精密なbenchmarkをとるためにgem（'benchmark/ips'）を導入
Warming up：iterations per second
Calculating：標準偏差
計測してみると純粋なrubyの方がやはり速度が出る

オブジェクトからの呼び出しやパーサーの処理の事を考えて１＋１とCRuby.add(1, 1)で計測するのは
公平じゃないと判断し、同じような呼び出し方でRuby.add(1, 1)で計測することにした。
結果、Crubyのほうが若干速度が出た。